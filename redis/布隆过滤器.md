# 原理
```
结构
    n个hash函数
    一个很长的整形数组
添加流程
    用n个hash函数计算key，得到n个hash值
    用n个hash值分别和数组取余得到对应的数组位置
    将对应的数组位置值，置为1
判断流程
    key的计算流程，如添加流程，如果对应的数组都为1
        侧值可能存在
        否则，值一定不存在
redis4.0后，官方已支持布隆过滤器了
    安装bf插件后
        127.0.0.1:6379> bf.add www.biancheng.net hello
        
```